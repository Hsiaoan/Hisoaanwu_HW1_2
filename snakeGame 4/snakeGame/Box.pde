
class Box {
  PVector originPoint;
  PVector size;
//  int boxID;
  
  Box (float x, float y, float a, float b) {
    originPoint = new PVector(x, y);
    size = new PVector(a, b);
//    boxID = m;
  }
  
}
